extends layout

block content
    h1= article.title
    h5 napsal #{author}
    h5 pro #{worker}
    p= article.body
    hr
    if user
        if user.id ==article.author
            a.btn.btn-default(href="/articles/edit/" + article._id) Upravit
            a.btn.btn-danger.delete-article(href="#", data-id=article._id) Smazat